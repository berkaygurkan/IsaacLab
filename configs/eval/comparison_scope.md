# Comparison Scope (Conference Stage)

Current comparison scope is intentionally bounded.

Use evaluation organization that supports comparison across:
- healthy baseline
- teacher-stage outputs
- student-stage outputs
- residual-stage outputs

Current fault comparison scope:
- none
- joint_lock
- torque_scale
- limited free_swing_surrogate only if explicitly needed

Current metric comparison scope:
- success rate
- return
- recovery time
- tracking error

Deferred by default:
- uncertainty metrics
- safety metrics
- later-phase real-robot reporting breadth
