# Checkpoint Pointers

Raw RSL-RL outputs remain under `logs/rsl_rl/`.

Repo-stable checkpoint pointers are written by `trainers/rsl_rl_train.py` to:

`checkpoints/<method>/<stage>/<fault>/seed<id>/latest_checkpoint.yaml`

